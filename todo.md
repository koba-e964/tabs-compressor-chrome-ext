# TODO

- [ ] Add `README.md` with install and usage steps for the extension.
- [ ] Add extension icons and reference them in `manifest.json`.
- [ ] Add `LICENSE`.
- [ ] Add `.gitignore` (at least `node_modules/`, `.DS_Store`).
- [ ] Document `pre-commit` setup (`pre-commit install`, `pre-commit run --all-files`).
